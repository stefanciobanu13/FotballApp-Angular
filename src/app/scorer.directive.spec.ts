import { ScorerDirective } from './scorer.directive';

describe('ScorerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScorerDirective();
    expect(directive).toBeTruthy();
  });
});
