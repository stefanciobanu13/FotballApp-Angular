export class Round {
  id: number;
  number: number;
  date: string;

  constructor(id: number, number: number, date: string) {
    this.id = id;
    this.number = number;
    this.date = date;
  }
}
