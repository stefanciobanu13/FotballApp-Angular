import { UpdateScoreDirective } from './update-score.directive';

describe('UpdateScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateScoreDirective();
    expect(directive).toBeTruthy();
  });
});
